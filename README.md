# WozzaWatch

A UK streaming chart PWA. Official public charts are used first where a compatible source is available; otherwise the app clearly labels its fallback source.

The ranking cache is refreshed automatically by GitHub Actions every six hours.
