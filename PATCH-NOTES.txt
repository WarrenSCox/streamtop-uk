WozzaWatch v5.3.42 — WozzaRead + News/Menu CRs

NEW: WozzaRead
- Adds read.html with two UK-only overall charts: BOOKS and AUDIOBOOKS.
- Books updater uses LoveReading's public Official UK Top 10 (trade-sales chart).
- Audiobooks updater uses Audible UK Best-selling / All categories.
- update-read.mjs refuses partial/uncertain results: exactly 10 ranked titles are required; otherwise the previous verified chart is retained.
- All Wozzas menu now links to WozzaRead.

CR FIXES
- All Wozzas menu is centred in the viewport from both access methods.
- WozzaNews paper plane is horizontally flipped.
- Metro categorisation tightened further and now includes RSS category metadata in classification; overly broad tech matches removed.

DATA NOTE
- Add update-read.mjs to the same scheduled GitHub Actions refresh pattern as update-news.mjs and commit read.json after a successful run.
