WozzaWatch v5.3.43 — WozzaRead Hotfix

- Fixes UK Books chart extraction by reading the ten ordered LoveReading /book/ entries from the Official UK Top 10 section only.
- Adds title/author extraction for Books and cover enrichment from each book detail page when the chart page does not expose a usable image.
- Reworks Audible parsing around the explicit numbered 1–10 headings instead of brittle list-item markup.
- Fetches audiobook cover artwork from Audible product metadata when it is not present beside the ranked heading.
- Removes the redundant UK SALES / AUDIBLE UK badge from the WozzaRead chart heading, eliminating the mobile overlap.
- Keeps fail-safe behaviour: an incomplete chart never replaces the last verified Top 10.
- Includes the existing master GitHub Actions workflow that runs update-read.mjs alongside rankings/news.
