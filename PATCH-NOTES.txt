WozzaWatch v6.1.1 — Refresh Button Removal CR

- Removed REFRESH STATS from the All Wozzas / extra menu on every page.
- The previous button only opened GitHub Actions; it did not force a refresh from inside WozzaWatch.
- No fake/manual refresh control remains in the app.
- A true in-app forced refresh would require a secure server-side trigger; no GitHub token is exposed client-side.
- All other v6.1.0 Read Polish CR features remain unchanged.

Previous v6.1.0 notes follow:

WozzaWatch v6.1.0 — Read Polish CR Pack

DONE
- WozzaRead source badge in the normal chart-heading position:
  Books = STATS FROM LOVEREADING; Audiobooks = STATS FROM AUDIBLE.
- Animated Read controls: book page flip; audiobook wire wiggle. Reduced-motion respected.
- Stronger missing-cover recovery for BOTH Books and Audiobooks:
  exact primary product artwork first, then strict title-matched Google Books (books) / Apple audiobook artwork (audio), then W fallback.
- Books can now be added to the existing Watchlist with the googly-eyes control.
- Watchlist gains a third Books tab and book counts. Book removal does not send books into the film/TV Watched pile.
- WozzaRead double-tap and swipe toggle Books <-> Audiobooks; links/buttons remain protected.
- All Wozzas menu gains REFRESH STATS, taking you straight to the repository's manual Update WozzaWatch rankings workflow without exposing a GitHub token.

NOT BUNDLED / SAFETY NOTE
- HBO 403 resilience is NOT replacing update-data.mjs in this patch because the v6.0.2 patch archive did not contain the current live updater. Bundling an older Library copy would regress the newer Prime/HBO work. Keep the repository's current update-data.mjs unchanged for this upload.
- Customisable menus deliberately excluded per CR.
